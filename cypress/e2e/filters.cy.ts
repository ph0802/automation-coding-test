describe('Category Filters', () => {
  it.todo('Should validate that all checkboxes are checked by default', () => {

  });

  it.todo('Should validate that checkboxes filter content appropriately', () => {
    // Compare against intercepted real data
    // This test may need to be split into multiple for each category depending on how you structure it
  });

  it.todo('Should validate that unchecking all checkboxes re-enables all', () => {

  });

  it.todo('Insert additional tests here and below', () => {
    
  });
});
