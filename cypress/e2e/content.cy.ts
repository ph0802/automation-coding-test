import { RACING_CATEGORIES } from "../config/constants";

describe('Page Content', () => {
  it.todo('Should correctly display page title', () => {
    // Next To Go Races
  });

  it.todo('Should display expected values for race row contents', () => {
    // Race number, venue name, time to jump
  });

  it.todo('Insert additional tests here and below', () => {
    
  });
});
